# ============================================
# productsPositions - Dependências Python
# ============================================
# Sistema de Gestão de Posições e Produtos
# Instalar com: pip install -r requirements.txt
# ============================================

# --- Core ---
pandas>=2.0.0            # DataFrames, queries SQL, leitura de Parquet
numpy>=1.24.0            # Cálculos numéricos (ATR, trailing stop)

# --- HTTP / APIs ---
requests>=2.28.0         # Chamadas a APIs (CoinGecko, Binance, Bitget)

# --- Configuração ---
python-dotenv>=1.0.0     # Leitura de variáveis de ambiente (.env)

# --- Armazenamento ---
pyarrow>=12.0.0          # Leitura de arquivos Parquet (dados de preço CoinGecko)
openpyxl>=3.1.0          # Export Excel (rentabilidade, posições)

# --- Visualização (analytics / notebooks / dashboard) ---
plotly>=5.15.0           # Gráficos interativos (charts.py, notebooks Jupyter)

# --- Opcional: Notebooks Jupyter ---
# jupyter>=1.0.0         # Descomente se for usar notebooks para análise
